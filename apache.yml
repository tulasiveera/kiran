---
- hosts: webservers
  become: true
  tasks:
  - name: installing pkg
    apt: 
      name: "httpd"
      update_cache: yes
      state: "present"
  - name: start apache server
    service:
      name: httpd
      state: started
      enabled: true
  - name: deploy static website
    copy:
      src: index.html
      dest: /var/www/html/
...      
 
 
